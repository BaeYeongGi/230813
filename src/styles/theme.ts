import { Theme } from '@emotion/react';

export const lightTheme: Theme = {
	mode : {
		text: '#222',
		background:'#fff'
	}
} 

export const darkTheme: Theme = {
	mode: {
		text: '#fff',
		background: '#333'
	}
}